# Desenvolvimento: npm run dev sobe o PartyKit na 1999
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
NEXT_PUBLIC_PARTYKIT_PARTY=main

# Produção (Vercel): host retornado por `npx partykit deploy` — sem https://
# Obrigatório no deploy do Next; o domínio vercel.app não atende WebSocket do PartyKit.
# NEXT_PUBLIC_PARTYKIT_HOST=seu-projeto.seu-user.partykit.dev
